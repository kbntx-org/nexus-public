apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudflare-controller-secret-sa
automountServiceAccountToken: false
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: cloudflare-controller-secret-store
spec:
  provider:
    vault:
      auth:
        kubernetes:
          mountPath: kubernetes
          role: cloudflare-controller
          serviceAccountRef:
            name: cloudflare-controller-secret-sa
            audiences:
              - vault
      path: platform
      server: https://vault.kbntx.com
      version: v2
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflare-controller-secret
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: cloudflare-controller-secret-store
    kind: SecretStore
  target:
    name: cloudflare-controller-secret
  dataFrom:
    - extract:
        key: cloudflare-controller
