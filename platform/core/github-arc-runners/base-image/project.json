{
  "name": "nexus-ci-toolkit",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "platform/core/github-arc-runners/base-image",
  "tags": ["docker-base-image"],
  "targets": {
    "build-ci": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tools/docker-build-and-push.sh platform/core/github-arc-runners/base-image/Dockerfile platform/core/github-arc-runners/base-image kbntx/nexus-ci-toolkit:1.7.2 --build-arg RUNNER_VERSION=2.337.0 --build-arg NODE_VERSION=$(jq -r '.engines.node | ltrimstr(\"^\")' package.json) --build-arg PNPM_VERSION=$(jq -r '.packageManager | split(\"@\")[1]' package.json) --build-arg GO_VERSION=$(jq -r '.engines.go' package.json)"
      }
    }
  }
}
