{
  "name": "nexus-ci-toolkit",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "platform/core/github-arc-runners/base-image",
  "tags": ["docker-base-image"],
  "targets": {
    "build-ci": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tools/docker-build-and-push.sh platform/core/github-arc-runners/base-image/Dockerfile platform/core/github-arc-runners/base-image kbntx/nexus-ci-toolkit:latest --build-arg NODE_VERSION=$(yq '.tools.node' mise.toml) --build-arg PNPM_VERSION=$(yq '.tools.pnpm' mise.toml) --build-arg GO_VERSION=$(yq '.tools.go' mise.toml)"
      }
    }
  }
}
