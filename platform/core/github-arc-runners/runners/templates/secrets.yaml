apiVersion: v1
kind: ServiceAccount
metadata:
  name: github-arc-runners-secret-sa
automountServiceAccountToken: false
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: github-arc-runners-secret-store
spec:
  provider:
    vault:
      auth:
        kubernetes:
          mountPath: kubernetes
          role: github-arc-runners
          serviceAccountRef:
            name: github-arc-runners-secret-sa
            audiences:
              - vault
      path: platform
      server: https://vault.kbntx.com
      version: v2
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: github-arc-runners-secret
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: github-arc-runners-secret-store
    kind: SecretStore
  target:
    name: github-arc-runners-secret
  dataFrom:
    - extract:
        key: github-arc-runners
