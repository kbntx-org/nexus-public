apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-dns-secret-sa
automountServiceAccountToken: false
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: external-dns-secret-store
spec:
  provider:
    vault:
      auth:
        kubernetes:
          mountPath: kubernetes
          role: eso
          serviceAccountRef:
            name: external-dns-secret-sa
            audiences:
              - vault
      path: platform
      server: https://vault.kbntx.com
      version: v2
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: external-dns-secret
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: external-dns-secret-store
    kind: SecretStore
  target:
    name: external-dns-secret
  dataFrom:
    - extract:
        key: external-dns
