apiVersion: v1
kind: ServiceAccount
metadata:
  name: cert-manager-secret-sa
automountServiceAccountToken: false
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: cert-manager-secret-store
spec:
  provider:
    vault:
      auth:
        kubernetes:
          mountPath: kubernetes
          role: eso
          serviceAccountRef:
            name: cert-manager-secret-sa
            audiences:
              - vault
      path: platform
      server: https://vault.kbntx.com
      version: v2
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cert-manager-secret
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: cert-manager-secret-store
    kind: SecretStore
  target:
    name: cert-manager-secret
  dataFrom:
    - extract:
        key: cert-manager
