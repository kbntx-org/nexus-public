apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudflared-secret-sa
automountServiceAccountToken: false
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: cloudflared-secret-store
spec:
  provider:
    vault:
      auth:
        kubernetes:
          mountPath: kubernetes
          role: cloudflared
          serviceAccountRef:
            name: cloudflared-secret-sa
            audiences:
              - vault
      path: platform
      server: https://vault.kbntx.com
      version: v2
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflared-secret
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: cloudflared-secret-store
    kind: SecretStore
  target:
    name: cloudflared-secret
  dataFrom:
    - extract:
        key: cloudflared
