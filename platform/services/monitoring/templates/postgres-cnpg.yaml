apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: grafana-postgres-cnpg
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:17.9-system-trixie

  storage:
    size: 10Gi
    storageClass: {{ .Values.postgresCluster.storageClass | quote }}

  enableSuperuserAccess: true
  superuserSecret:
    name: monitoring-secret-user-credentials
  bootstrap:
    initdb:
      database: grafana
{{- if .Values.backup.enabled }}

  backup:
    barmanObjectStore:
      destinationPath: {{ .Values.backup.destinationPath | quote }}
      endpointURL: {{ .Values.backup.endpointURL | quote }}
      s3Credentials:
        accessKeyId:
          name: monitoring-secret
          key: backupAccessKeyId
        secretAccessKey:
          name: monitoring-secret
          key: backupSecretAccessKey
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: 4d
{{- end }}
{{- if .Values.backup.enabled }}
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: grafana-postgres-cnpg-daily
spec:
  schedule: "0 0 6 * * *"
  backupOwnerReference: self
  cluster:
    name: grafana-postgres-cnpg
{{- end }}
