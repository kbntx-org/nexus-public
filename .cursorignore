# Prevent Cursor AI from reading sensitive files

# Terraform variable files (may contain secrets)
*.tfvars
*.tfvars.json

# Environment files
.env
.env.*
*.env

# Terraform state (contains sensitive data)
*.tfstate
*.tfstate.*

